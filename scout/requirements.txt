# Core
keepa==1.5.0          # sanctioned Amazon data layer (REQUIRES a paid Keepa key). Pinned exact
                      # (review fix, 2026-07-07): was >=1.3, so every CI run silently installed
                      # whatever the latest release happened to be — this dev environment tested
                      # against 1.3.15 while a live GitHub Actions run installed 1.5.0 fresh, and
                      # the package's internal module structure changed substantially between
                      # them (a single interface.py split into keepa_sync.py/keepa_async.py/
                      # models/). Bump deliberately, with local tests re-run against the new
                      # version first, never silently.
requests>=2.31        # Discord webhook posting
python-dotenv>=1.0    # load .env

# Optional learning layer (scout runs on the rule score without these,
# but train.py / the model need them)
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24

# The ranker challenger model (scout/train_ranker.py) — LightGBM handles missing (NaN) feature
# values natively, which is why vectorize_one() imputes to NaN instead of a fabricated 0.0
# (Session 55 review fix). Needed here (not just requirements-train.txt) because
# scout/pipeline.py's live scoring path (challenger_score) unpickles + calls .predict_proba() on
# whatever model a human has promoted via ai-brain.json scoring.rankingChampion — degrades to
# an honest "falling back to the rule score" if absent, never a crash (see train_ranker.
# load_challenger's own try/except), but the promoted challenger only actually SCORES with this
# installed.
lightgbm>=4.0

# Optional: LLM analyst pass (Scout Agent Build Plan Prompt S1/S3) — skips gracefully without
# an ANTHROPIC_API_KEY even if installed.
anthropic>=0.39

# Optional: free signal-type features (Session 55, scout/signals/). pytrends is an UNOFFICIAL
# Google Trends scraper — scout/signals/trends.py guards the import and degrades to an honest
# no-op (collect_weekly/backfill_term return status="disabled") if it's absent; live feature
# READS (trends_features()) don't need it at all, only the collector/backfill do. pandas is
# already a transitive dependency via scikit-learn.
pytrends>=4.9

# Data lake (DATA_ENGINE_PLAN.md V0) — append-only zstd Parquet raw-response archive.
# scout/datalake.py no-ops cleanly if pyarrow is absent (archiving is best-effort), so these are
# optional, but required for the lake to actually record. Pinned to the last Python-3.9-compatible
# releases (this machine runs 3.9.12): pyarrow 17.x is the final 3.9 line; zstandard ships 3.9 wheels.
pyarrow>=17,<18 ; python_version >= "3.9"
zstandard>=0.22

# Optional: read-only operator MCP server (Scout Agent Build Plan Prompt S4).
# REQUIRES Python 3.10+ — `pip install mcp` fails on Python 3.9 with no matching distribution.
# mcp>=1.0 ; python_version >= "3.10"
