# Weekly command reviews (append, newest first)

Generated by the `fba-weekly-command-review` scheduled task (Cowork). One honest summary per week — sources: AI_COLLABORATION_JOURNAL.md (last 8 days), brain-proposals.md, ops-report.md, threshold-tuning-report.md, calibration-report.md, ranker-report.md, HUMAN_TODO.md.

## 2026-07-13 — Weekly command review

**Discord delivery status:** FAILED this run — the Cowork sandbox proxy blocked all outbound HTTPS to Discord (403 Forbidden on CONNECT, via both `urllib` and `curl`), the same restriction the 2026-07-06 run hit. The full summary is below; a Claude Code session (real network access) can re-post it if wanted.

**Shipped this week (07-06 → 07-13):**

- **Deal-to-ASIN matcher (Prompt D2) built for the first time** (Session 64), then extended (Session 66) to use the free SP-API Catalog + eligibility layer ahead of Keepa — designed to cut per-deal Keepa cost from ~30 tokens to ~1–5. Implemented + unit-tested (full suite 1108 green). **NOT live-verified** — SP-API credentials are still placeholders, so every real SP-API call currently fails auth and degrades to Keepa-only.
- **First live matcher smoke test** (dry-run, 3 deals, Session 65) ran cleanly; the token guard behaved exactly as designed (capped/skipped as the bank thinned). Too small to judge match quality.
- **ML label redefinition:** `would_have_profited` now mirrors the real buy gate (profit ≥ $3 AND ROI ≥ 30%), fixing the ~91% positive-rate artifact — for windows built going forward only (historical rows not yet backfilled).
- **$8–60 price-band** added as a soft scored adjustment; **Review Queue UI** rebuilt to show labeled Sell/Buy/Profit/ROI/BSR + an honest "ESTIMATED — no source" badge.
- **Keepa throughput plan finally deployed** — the crew found 5 commits had never been pushed (a recurring "journaled a commit as deployed without confirming the push" gap); pushed and live-confirmed via the Actions run log.
- **Brain proposals applied:** LEGO + Under Armour moved friendly → avoid; `assumedDailyTokens` 7500 → 1500; `kitchen` dropped from sampling categories (never resolved to a real Keepa root).

**Pending brain proposals:** 2 pending on the latest run (2026-07-13 02:00 UTC), both recurring low-signal telemetry: (1) avg Keepa usage ~60/day vs the 1500 assumed ("worth reviewing"), (2) a knowledge-base threshold spot-check ("manual review suggested"). Every substantive proposal this week was reviewed and applied. **Most actionable:** eyeball the RAG answer for current BSR/ROI/profit thresholds against `ai-brain.json` — a ~2-minute manual check.

**KPI status:** No realized outcomes yet — 40 leads, 0 with an outcome. Calibration: 0 trainable labeled rows (< 30 required), so the scout runs on the transparent rule score alone; no model fit or promoted. Prediction hit-rates still unavailable per the ops report.

**Blocked on Mehmet:**

- **Provision real SP-API credentials** (Seller Central → Develop Apps) — the single biggest blocker; the matcher's eligibility + token-savings path is built but dormant until this exists.
- **ANTHROPIC_API_KEY** — unlocks LLM match verification, the `/proposals` drafting flow, and the analyst/reflect passes.
- **Open question (Session 65 Item 3):** confirm price-band/offer-count are soft-by-design (not hard gates) before any scoring-architecture change is made in response to Cowork's "gate leak" framing.
- **The irreducible human task:** run 10–20 real analyses through the Find page and record real buy/pass outcomes — every ML claim stays "architectural, not proven" until outcomes exist.

**Recommended focus next week:** Provision SP-API credentials and run the first real small-limit matcher pass to live-verify the eligibility gate + token savings — this unblocks the largest built-but-dormant capability. In parallel, begin the 10–20 real analyses to seed the first ground-truth outcomes.

---

## 2026-07-06 — Weekly command review

**Discord delivery status:** FAILED this run — the Cowork sandbox proxy blocked all outbound HTTPS (even example.com), so the embed could not be POSTed to the daily-digest webhook. The full summary is below instead; a Claude Code session (which has real network access) can re-post it if wanted.

### Shipped this week (Jun 29 – Jul 6, Sessions ~06–55)

The system went from local-only to a live cloud pipeline:

- Data engine V0–V2 **implemented + tested** (raw data lake, shadow-outcome tracker, backtest/training loop), and **first live data**: real Keepa pull + first model training with metrics posted to Discord (Session 51, Jul 5).
- Repo pushed to GitHub (`EptSniper/fba-system`). A GitHub account-level Actions gate was diagnosed (Session 52), then permanently bypassed by making all workflows marketplace-action-free — **deal-watch and train-ranker both ran fully green live** (one real run: 1,000 deals upserted to Supabase, 22 fresh deal hints) (Session 53).
- Hourly Keepa burst collector + raw-inbox mailbox **built, tested, and live-dispatched twice**; a real stale-token-read bug was found and fixed on the first real dispatch (Session 54).
- Session 55 (Jul 6): Keepa **overdraw guard** after the balance hit −100 tokens (implemented + tested), train-ranker cadence → every 6 hours with a skip-if-unchanged fingerprint guard, brand-agnostic sampling overhaul (Keepa `/deal` firehose, stratified composition, bronze labels excluded from the training target), and free signal features (Google Trends / calendar / eBay sold-comps) — **built + tested, NOT live-verified** (Keepa balance was negative all session). 759 tests passing project-wide, 4 commits pushed.
- Earlier in the week: Top-100 deal watch built + live-verified (Sessions 43–44), control-center cockpit CC1/CC2 (live Supabase read layer, Review Queue, Morning Brief, /proposals approve flow with human confirm), Discord multi-channel routing, knowledge-exam baseline harness, deals-blog deployed.

### Pending brain proposals

1 unique proposal pending (repeated across 5 proposal runs, latest 2026-07-06): manually compare the RAG's current OA-threshold answer (`python knowledge-rag/ask.py "current BSR ROI profit threshold"`) against `ai-brain.json`. Most actionable decision on deck (from ranker-report.md, not the proposals file): the **challenger ranker beat the deterministic champion on held-out AUC (0.51 vs 0.33)** — backtest-tier labels only (weakest tier, no real execution/sell-through), so treat with caution; flipping `scoring.rankingChampion` is a human-only brain edit.

### KPI status

No realized outcomes yet — ops-report (Jul 6): 40 leads total, 0 with an outcome; prediction hit rates unavailable (report cites KEEPA_KEY not configured for the local re-fetch path). Calibration report: 0 trainable labeled rows (30 required) — the scout honestly runs on the rule score alone; nothing promoted. No real profit/ROI numbers exist. (Minor observation: ops-report.md is appending many duplicate identical blocks per day — worth a look.)

### Blocked on Mehmet

- Apply Supabase migrations **011 + 012** via the SQL editor — Session 55's sampling/Trends code degrades gracefully until then (Session 55 next-safe-step).
- `ANTHROPIC_API_KEY` (HUMAN_TODO #1) — unlocks the analyst second-opinion pass, weekly reflection, and /proposals AI drafting.
- Rotate the exposed Supabase service_role key (#3 — flagged overdue).
- eBay developer keys (#2b), SP-API developer registration (#4), blog domain purchase → Best Buy API + Impact/CJ affiliate applications (#5), healthchecks.io dead-man's switch (#6), optional Discord webhook rotation (#7).
- **#8 — the highest-leverage item: run 10–20 real analyses through the Find page and record outcomes.** Every learning loop (calibration, ranker validation, threshold tuning) is fuel-starved without real decisions.

### Recommended focus for the coming week

Apply migrations 011/012, confirm the first real token-spending hourly burst once the Keepa balance recovers (refilling at 1 token/min from negative — watch the `runs` table or the daily digest), then start putting real buy/pass decisions through the Find page: the ML stack is now fully live end-to-end but has zero real outcomes to learn from.

---
