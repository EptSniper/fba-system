"""scout/signals/ — free signal-type features (Session 55): calendar.py (pure functions, no
API), trends.py (Google Trends via pytrends), ebay.py (eBay Browse API, key-gated). Each module
is independently importable; scout/collect_hourly.py and scout/backtest.py attach their outputs
onto the SAME PRE_DECISION_FEATURES-projected snapshot every other feature already flows through.
"""
