# Claude Code directive — execute the Keepa throughput + unbiased-collection plan

Paste the block below into Claude Code (in the Amazon FBA repo). It routes through the ML crew per the CLAUDE.md ML mandate, runs live where Cowork can't, and keeps every guardrail. Do the steps in order; stop for Mehmet's approval where noted.

---

You are working in the Amazon FBA repo. Read `KEEPA_THROUGHPUT_PLAN.md` first — it's the spec. Goal: raise unique-ASIN/day toward the Pro-plan ceiling and reach 10,000 unique ASINs without a plan upgrade, keeping collection brand-agnostic, with leakage/overfitting/guardrails signed off. Route through the ML crew (`fba-ml-lead` to coordinate, `fba-scout-strategist`, `fba-ml-data-engineer`, `fba-leakage-auditor`, `fba-ml-evaluator`, `fba-ml-guardian`) per the ML mandate in CLAUDE.md. Implementation via `fba-coder`, review via `fba-code-reviewer`, tests via `fba-qa-tester`. Hard rules, non-negotiable: hard gates stay outside ML; the ranker only ranks a shadow queue; never auto-promote (`scoring.rankingChampion` stays `rule`); never auto-buy; no secrets in tracked files; every `ai-brain.json` edit goes through `fba-brain-updater` and waits for Mehmet's approval. Log a journal entry at the end via `fba-session-journal`.

Do these in order, each with a before/after measurement (new-ASINs/day from `backtest_rows.created_at`, and tokens spent per new ASIN):

1. **Reliability first (biggest current leak).** 2026-07-06 and 07-07 collected ZERO new ASINs — whole days of the ~1,440-token budget were lost because a missed hourly run overflows the 60-token bank. Use `fba-debugger` to find why the collector didn't fire those days (GitHub Actions cron drop vs the local `scripts/dispatch_keepa_collect.ps1` dispatcher not running). Make the hourly dispatch reliable 24/7, and wire the existing `HEALTHCHECK_URL` / Discord `SYSTEM_HEALTH` webhook to alarm when a day collects fewer than a threshold of new ASINs. Verify by watching two full days with no gaps.

2. **Stop paying history tokens for zero-yield ASINs.** In `scout/backtest.py run_backtest`, before the `query_history` batch: (a) filter `todo` against ASINs already in `backtest_rows` (free DB check) so a 1-token re-pull never buys an already-known ASIN; (b) MEASURE the 0-row rate — how many pulled ASINs produce 0 rows because they have `< MIN_HISTORY_DAYS` (90d) of history (dealfeed "recently changed" listings are often too new). If that rate is material, add a cheap age/first-tracked pre-screen or bias discovery toward established listings. Have `fba-ml-data-engineer` confirm this raises rows-per-token without touching the leakage boundary; `fba-leakage-auditor` signs off that the pre-filter uses no post-decision info.

3. **Route ~all tokens to 1-token history during the collection sprint.** `corpusAcceleration` is already ON (Tier 2 skipped while a backlog exists) — keep it. Propose via `fba-brain-updater` (Mehmet-approved) temporarily lowering `TIER1_RESERVE_FRACTION` 0.25→0.15 in `scout/collect_hourly.py` so 4-token shadow-recheck enrich doesn't skim a quarter of every hour; do NOT zero it (shadow validation is how the ranker eventually earns promotion). Keep a small dealfeed backlog primed so `sampling_skipped` stays true and each run converts ~100% of its cap into history rows.

4. **Add storefront stalking as a second cheap breadth source.** `keepa_client.seller_asins()` already exists (guarded). One `seller_query` returns hundreds of ASINs for ~10 tokens — brand-agnostic if sellers are rotated, and it reaches beyond "on-deal" products, sustaining new-ASIN yield as the dealfeed overlaps day to day. Have `fba-architect` confirm the design, then `fba-coder` wire a rotating-seller cursor (same persisted-cursor pattern as `dealfeed_cursor.json` / `explore_cursor.json`) that feeds ASINs into the backtest backlog tagged `sample_source="storefront"`. `fba-scout-strategist` picks a diverse, brand-agnostic seller-rotation set. Add tests via `fba-qa-tester`.

5. **Keep the discovery surface wide.** Verify the dealfeed secondary-axis range filters (`isRangeEnabled`/`isFilterEnabled`) actually take effect live — read `dealfeed_yield_stats.json` for per-band yields > 0; a silently-ignored filter is ml-doctrine §7's "green tests, broken machine." Confirm all 18 `learning.sampling.categories` are resolving to Keepa root ids (not silently degrading to unfiltered).

6. **Guardrail + honesty pass before anything ships.** `fba-leakage-auditor`: confirm the poisoned-future regression test landed and that none of the above introduced train/test or temporal leakage. `fba-ml-evaluator`: the corpus is 87.3% positive, all weakest `backtest` tier — enforce that evaluation stays on AUC + precision/lift@top vs base rate + time-held-out + per-brand/category slices + bootstrap CI (never raw accuracy), and track positive-rate drift as volume grows. `fba-ml-guardian`: re-confirm hard gates outside ML, shadow-only, no auto-promote/buy, overdraw guard intact. `fba-code-reviewer` on every diff.

7. **Re-measure and report.** After 3–4 days, report new-ASINs/day and tokens/new-ASIN. If sustained < ~400/day after 1–4, escalate the plan-upgrade decision in `KEEPA_THROUGHPUT_PLAN.md` §4 (Keepa API tier unlocks Product Finder at ~0.001 tok/ASIN; or a second key) — present the numbers to Mehmet, do NOT purchase anything.

Do not promote the ranker regardless of any single-run win. Finish with a `fba-session-journal` entry: what you implemented vs tested vs configured vs deployed vs planned, the measured throughput delta, and the exact next safe step.
